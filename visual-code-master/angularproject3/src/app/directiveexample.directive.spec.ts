import { DirectiveexampleDirective } from './directiveexample.directive';

describe('DirectiveexampleDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveexampleDirective();
    expect(directive).toBeTruthy();
  });
});
